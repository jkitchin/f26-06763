# Week 03 — Pipelines & Processing
**Arc:** Data Systems · **Lectures:** L5–L6

**Prerequisites (from prior weeks):** Reproducible `uv` project and MLflow basics (Wk1); ability to load data from PostgreSQL/Parquet/DuckDB (Wk2). Comfort writing Python functions and running scripts.

## Module learning objectives
- Build a batch data pipeline as a sequence of pure, testable transform steps with clear inputs/outputs.
- Compare pandas and Polars idioms and explain when lazy/columnar execution matters.
- Reason about scaling out with Dask/Spark at a concepts level and recognize when you do *not* need them.
- Explain streaming vs. batch for sensor data and the core streaming concepts (windows, watermarks, late data).
- Encode data-quality expectations as executable checks with pandera and/or Great Expectations, and fail a pipeline on violation.

## Lecture L5 — Dataframes, batch pipelines, and scalable processing (concepts)
**Objectives:**
- Write idiomatic transformations in pandas and Polars and read/benchmark both.
- Structure a batch pipeline into reproducible, parameterized stages.
- Explain the distributed-processing model (partitioning, lazy graphs) without needing a cluster.

**Topics:**
- Dataframe fundamentals: vectorization, `groupby`/aggregate, joins, reshaping (long↔wide) for sensor data; avoiding row-wise Python loops.
- Polars: lazy vs. eager, expressions, query optimization; why columnar + multithreaded execution is fast; interop with Arrow/Parquet.
- Batch pipeline design: extract → clean → transform → load stages, idempotency, deterministic outputs, caching intermediate Parquet.
- Orchestration awareness (concepts): what tools like Makefiles, Prefect, or Dagster add; a plain-Python DAG is fine for this course.
- Distributed concepts only: partitioning, shuffles, lazy graphs; Dask dataframe as "pandas that spills to disk/cluster," Spark's execution model. When the answer is "just use DuckDB/Polars on one big machine."

**Live demo / in-class activity:** Take the SECOM manufacturing dataset and build a 4-stage pipeline (load → drop constant/high-missing columns → impute → aggregate) written twice — once in pandas, once in Polars lazy — and benchmark. Then run a Dask demo that processes the same data in partitions to illustrate the out-of-core model (concept only, on the laptop).

**Engineering framing / dataset:** UCI SECOM dataset — 1,567 semiconductor manufacturing process runs, 590 sensor/process measurements per run, heavy missingness and many uninformative signals; a realistic dirty industrial feature matrix with a pass/fail label.

**Readings & resources:**
- Polars User Guide, "Concepts: Lazy API" and "Expressions."
- pandas documentation, "Group by" and "Reshaping."
- Dask documentation, "DataFrame — Best Practices."
- M. Kleppmann, *Designing Data-Intensive Applications*, Ch. 10 ("Batch Processing") — recommended.

## Lecture L6 — Streaming concepts and data validation
**Objectives:**
- Distinguish batch from streaming and identify when sensor systems require streaming.
- Explain windowing, event vs. processing time, watermarks, and late/out-of-order data.
- Author executable data-quality checks and integrate them as a pipeline gate.

**Topics:**
- Streaming for sensor/IoT data: bounded vs. unbounded data, at-least-once vs. exactly-once, backpressure; the log abstraction (Kafka/MQTT) at a concepts level.
- Windowing: tumbling/sliding/session windows; event-time vs. processing-time; watermarks and handling late-arriving sensor readings.
- Micro-batch as a practical middle ground; why most engineering teams start with scheduled batch.
- Data validation: schema checks (types, ranges, units), statistical expectations (nulls, uniqueness, distributions), physical-plausibility checks (temperature within instrument range, monotonic timestamps).
- Tools: pandera (schema-as-code, integrates with pandas/Polars) and Great Expectations (expectation suites, data docs); where checks live in a pipeline and how failures should behave (block vs. warn vs. quarantine).

**Live demo / in-class activity:** Write a pandera schema for the SECOM (or Intel Lab) data — column dtypes, value ranges, allowed nulls, a custom check that timestamps are monotonically increasing — then inject corrupt rows and watch validation fail loudly. Show how the check runs as a gate before the transform stage. Sketch a windowed aggregate over a simulated sensor stream.

**Engineering framing / dataset:** SECOM continued (validation of a wide, dirty sensor matrix) plus a simulated/real streaming sensor feed (e.g., replaying Intel Lab readings by timestamp) to make windowing and late data tangible.

**Readings & resources:**
- pandera documentation, "DataFrameSchema" and "Checks."
- Great Expectations documentation, "Core concepts: Expectations."
- Tyler Akidau et al., "The Dataflow Model" (VLDB 2015) or the "Streaming 101/102" articles — for windowing/watermarks.
- Apache Kafka documentation, "Introduction" (concepts only).

## Assignment
**A3 — Reproducible, validated data pipeline** — Build a multi-stage batch pipeline with pandas or Polars that ingests, cleans, transforms, and validates an engineering dataset, with pandera/GE checks that fail on bad data. Released L5, due ~1 week later. Full spec: `assignments/a03.md`.

## Teaching notes / common pitfalls
- Students write one giant function. Insist on small pure stages with typed inputs/outputs; it makes testing and caching possible.
- Polars lazy confuses newcomers — nothing runs until `.collect()`. Demo the query plan and the collect step explicitly.
- Do not let students spin up a real Spark/Kafka cluster; keep distributed and streaming strictly concepts + a laptop demo. The learning objective is judgment, not ops.
- Validation is often treated as decoration. Make them inject bad data and prove the pipeline halts. A check that never fails is untested.
- Leakage sneaks in during "cleaning" (e.g., imputing with global statistics computed over all data). Flag it here; it is the focus of Wk4.

# Week 04 — Time-series features and the ML workflow
**Arc:** Data Systems → Machine learning & deep learning · **Lectures:** L7–L8

**Prerequisites (from prior weeks):** Reproducible `uv`/git project (Wk1); storage in PostgreSQL/Parquet/DuckDB (Wk2); Polars pipelines and a validated batch pipeline (Wk3). Comfort writing Python functions. No machine learning is assumed: L8 introduces it.

**This week is the hinge of the course.** L7 is the last session of the Data Systems arc and L8 is the first session of the ML arc. L7 builds the table. L8 fits models to it. Keeping that line sharp is what stops the two sessions from colliding, because both are about the same table.

**Register.** Both sessions are introductory and should feel welcoming. The audience is mixed: some know process dynamics far better than they know software, and for several this is the first programming-heavy semester. **Assume no lab experience.** An earlier draft of L7 opened with "you have done this experiment with your hands," which is false for most of the room and reads as a lecture addressed to somebody else. L7 now opens by naming the column it is about, in the first sentence, and every hard idea arrives as a picture, a number computed live, or a demo that misbehaves. A student who can say "could I have printed this column at 2 PM that day" has the transferable skill; a student who can define persistence of excitation has a vocabulary they cannot use yet.

**Sentence length is a teaching decision here, not a style preference.** The notes hold a median sentence of about 17 words and no section opens with a story. Section headings are literal ("Why one valve position gives two temperatures", not "The loop that a blob hides"), because a student scanning for the part they missed cannot decode a riddle.

## Module learning objectives
These summarize the week. The per-lecture blocks below carry the text lifted verbatim into the notes and into `game/content/objectives.yml`; this list is not copied anywhere.

- Recognize when data comes from a dynamic process, and build a table that carries its past.
- Check that every column in a table could have been known when it would be used.
- Understand what supervised learning is and what fitting a model does.
- Evaluate a time-series model against a baseline rather than in isolation.

## Lecture L7 — Features for time-series models
**The session in one sentence:** a step test means taking a unit off setpoint and the archive is already on disk, so this session gets the time constant and the gain out of the archive instead, by putting one column back in the table.

**Objectives:**
- Explain why a dynamic process needs its own past in the feature table, and a steady-state measurement does not.
- Build a feature table from a sensor log in which every value in a row was knowable at that row's timestamp, dropping the rows whose past lies outside the record.
- Convert the two coefficients of a fitted first-order model into a time constant and a gain, and say whether they are physically plausible.

**Topics:**
- **One example, all session.** One loop, start to finish: the A and C feed of Tennessee Eastman, `xmv_4` (the valve) against `xmeas_4` (the flow it sets). `xmeas_4` is a FLOW, in kscmh, not a temperature. **Do not introduce a second dataset.** Earlier drafts opened on beer sales and then moved to a stirred tank; cold readers reported the session read as a demand-forecasting lecture for its first seven minutes. There is no tank and no beer anywhere in the material now.
- **Why the session exists, stated before anything else.** Feature engineering has no self-check: a model fits, reports R-squared 0.94, and that number is just as high for a table that used a future value, grouped on the wrong column, or leaned on a channel that was only repeating itself. So the session picks a loop whose answer *is* checkable, because a first-order process converts its coefficients into minutes and flow-per-percent, which a plant engineer can judge. The step-test-versus-logged-data trade is the *second* motivation, not the first. An earlier draft led with the trade and a cold reader could not say what the lecture was for until slide 20.
- **Continuous-time and discrete-time models.** The spine, and the thing that justifies the whole feature discussion. Every model they have written is continuous (`tau dy/dt = -y + K u`); their data is discrete, one row per three minutes. Discretize with **zero-order hold**, which is free on a plant because the control system writes one valve position per scan: the integration is exact and yields `y[t+1] = a y[t] + b u[t]` with `a = exp(-dt/tau)`, `b = K(1-a)`. Verified symbolically and numerically (matches a tight ODE solve to 2e-14; Euler's coefficient would be `1 - dt/tau = 0.717` against the true `0.7535`). **Then read the difference equation as a table specification:** it names exactly two columns, so the features are derived rather than guessed. Name **ARX(1,1)** here, at the equation. Invert to `tau = -dt/ln(a)`, `K = b/(1-a)`. The **lag plot** shows `a` as a slope before any fit.
- **Data leakage.** Defined as IBM defines it: training on information that would not be available when the prediction is due. Taught through the **credit card chargeback** example (a column filled in weeks after the fraud is decided), then the same shape in our own file: `faultNumber`, written down after the run, and the slow analysers, whose 15-minute result is stamped at 14:00 but not available until 14:15. Then IBM's two categories: **target leakage** (a table property, ours) and **train-test contamination** (needs a held-out score, so it is named and handed on). **Use "leakage", never "realizability".** Realizability is a real control term for whether a transfer function is proper, so it collides, and the course already teaches this as leakage in L9.
- **Run boundaries and the group key.** The file holds 192,000 rows that are 400 separate experiments, identified by the pair `(faultNumber, simulationRun)`, not by `simulationRun` alone. Measured: `shift(1)` leaves 1 null and fixes 0 of 399 boundaries, `.over("simulationRun")` leaves 20 and fixes 19, `.over("faultNumber", "simulationRun")` leaves 400 and fixes all. The middle case is the dangerous one because the nulls make it look like it worked, so teach **count the nulls against the number of experiments**. An earlier draft shipped `.over("simulationRun")` as correct; it was not. **This is notes-and-demo only: it has no slide any more.**
- **Common data preparation operations, rapid fire, about eight minutes.** All on the same file. **(a)** Build the clock, because the file ships `sample` as an integer and has no timestamps, then check the intervals with `pl.col("ts").diff().value_counts()`, since `shift(1)` reaches back one *row* and not three minutes. **(b)** `upsample` back onto a grid and decide the fill per column: carry the valve forward (a control system really does hold its last position, which is zero-order hold again) and leave the measurement null, keeping a mask either way. **(c)** `rolling_std` with the same `.over(GROUP)` as the shift. **(d)** The channel screen, `pl.col("^xmeas.*$").diff().eq(0).mean()`, which on one run returns 19 of 41 channels flat on more than 40 % of rows, at exactly 0.500 and 0.800: the 6- and 15-minute analysers from Downs and Vogel table 5 on a 3-minute grid. Cut (c) first if the block runs long.
- **Sampling interval against the time constant.** Three-minute samples against a 10.6-minute time constant is about **three and a half samples per time constant**, which is why consecutive rows barely differ and why guessing "no change" is already right to within 9 % of the channel's spread three minutes out.
- **Multiple lags and the regression vector.** One lag is a starting point, not a rule. The general ARX form, the **regression vector** phi(t), the **design matrix** Phi, and `y = Phi theta`, which `lstsq` solves at any width. Measured on this loop: adjacent lag columns correlate at **0.927** (0.996 on reactor pressure), so Phi goes near-singular. What that does, measured: the one-lag `y[t]` coefficient of **0.753** becomes **0.417** at three lags, with the remainder landing on `y[t-1]` and `y[t-2]`. **Do not claim the coefficients blow up or alternate in sign on this data. They do not.** The real cost is that no single coefficient converts to a time constant any more.
- **Model structures: ARX, NARX, state space, RNN.** The ladder, in one notation: ARX(1,1) is the discrete first-order lag; widen it for ARX(na,nb); add dead time for FOPDT; make the function nonlinear for NARX; write it with a vector state for state-space; make that update nonlinear for an RNN, and add gates for an LSTM. The payoff line: `y[t+1] = a y[t] + b u[t]` and `h[t+1] = tanh(W h[t] + U u[t] + b)` are the same line of arithmetic. Every model on that ladder eats the same feature table, which is the argument for spending a session on the table.
- **Limitations: what the data cannot answer.** Two failures that arrive as plausible numbers. **The valve that never moved**: rank drops to 1, gain returns 0.000, and no feature engineering or bigger model recovers it because the information was never collected. Name **persistent excitation** once, after the concrete case. A single step is enough; only a genuinely constant input fails. **Closed-loop identification**: fitting a controlled loop returns a coefficient with the wrong sign, because the controller moved the valve *because* the measurement moved. Under tight control with no external excitation the direct estimate is pulled toward the negative inverse of the controller, so you identify your own PID tuning. **This is the session's required case study and must never be cut.**
- **One practical warning worth keeping.** `lstsq` needs an explicit `rcond`. A constant column is exactly zero only after mean-centering, and at the default tolerance the solver divides by rounding dust and returns a gain of nine hundred million instead of zero.

**Live demo / in-class activity:** ONE notebook, 27 cells, deliberately short, and it does **not** simulate anything. Four steps: load one run and build the clock; build an ARX regressor matrix twice (n_a=1,n_b=1 then n_a=3,n_b=2) and **print both matrices as labelled tables before fitting**, because seeing phi(t) laid out is the thing prose cannot do; solve with `lstsq` and convert (`a = 0.7529`, `b = 0.0322`, so tau = 10.6 min and K = 0.1304); then freeze the valve and refit, and watch the gain go to zero. An earlier version ran 63 cells over two datasets and did not fit the session.

**The demo no longer writes a Parquet file.** It used to hand L8 a 191,600-row feature table. It does not any more. See the L8 block below.

**No clicker questions.** All three were cut for time during the rebuild: the loop question
went last, on 2026-09-16. The standings slide went with them, and `clicker-slide.js` is still
loaded at the end of the deck so that pasting a question back in just works. If you want one
back, the loop question was the strongest: two points from the ramp, `xmv_4` reads 57.6 % on
both, `xmeas_4` is 8.58 on one and 9.00 on the other, what is wrong. Answer B, nothing is
wrong, the loop had not settled.

**Engineering framing / dataset:** One loop, all session: the A and C feed of Tennessee Eastman (Rieth et al. 2017, CC0), `xmv_4` against `xmeas_4`, from the same file L5 uses. TEP is the right plant because `xmv_*` (manipulated) and `xmeas_*` (measured) label inputs and outputs in the column names, and because the analyser channels make the channel screen real rather than illustrative. **Two figures are simulated and are labelled as such in both the notes and the deck**: `feed-loop.png` (the hysteresis loop) and `feed-lag-plot.png`, both generated at the time constant and gain measured from the plant's own data, at its own operating point (57.6 % valve, 8.79 flow), because no operator will ramp a live feed valve to produce them. Measured and worth knowing: the plant data *cannot* draw that loop, because `xmv_4` is under automatic control and rattles every sample, giving a cloud with correlation +0.69. That is not a defect, it is the closed-loop block arriving as a picture early in the deck. The flowsheet figure (`tep-flowsheet.png`, L5's P&ID annotated with `xmv_4`/`xmeas_4` and the analyser block) is on slide 6, where TEP is first named. C-MAPSS is not used here: FD001 has no manipulated input at all, with `setting3` equal to 100.0 in every one of its 20,631 rows, `setting1` spanning +/-0.0087, and `setting2` taking 13 distinct values across +/-0.0006.

**Readings & resources:**
- APMonitor, "Auto-Regressive Time Series Model" (<https://apmonitor.com/dde/index.php/Main/AutoRegressive>). The closest thing to this lecture anywhere online, and it is aimed at engineers: AR, then adding an input, then ARX, on a benchtop heater with a thermocouple. Fifteen minutes.
- scikit-learn, "Lagged features for time series forecasting" (<https://scikit-learn.org/stable/auto_examples/applications/plot_time_series_lagged_features.html>). Builds lag columns in Polars, which is the same tool this session uses.
- IBM, "What is a time series model?" (<https://www.ibm.com/think/topics/time-series-model>). A gentle overview if the vocabulary is new.

## Lecture L8 — The machine learning (ML) workflow I: Time series

**Rewritten 2026-09-16** for issue #134. The session is now about time-series modeling in
general, taught through h-step forecasting, and it is the session A4 is built on. TEP stays the
only real dataset; other fields (finance, electricity load, weather) appear as short contrasts
next to each idea, and a *simulated* random walk stands in for a price series so no market
data is downloaded. Kalman filtering and PCA T²/SPE were proposed in #134 and deliberately left
out: each is a second big idea in a 90-minute session.

**The session in one sentence:** a forecast is a supervised learning problem with a horizon
attached, and it is only worth anything if it beats persistence and the mean at that horizon
on data the model never saw in time order.

**Objectives:**
- Frame a time-series forecast as a supervised learning problem, stating the horizon and what is known when the forecast is made.
- Compare a forecasting model against persistence and mean baselines across horizons, and say whether the model earned its place.
- Choose between direct and recursive multi-step forecasting, and explain why recursive errors grow with the horizon.
- Evaluate a forecaster with a time-ordered split whose gap is at least the horizon, and explain why a shuffled split inflates the score.
- Use forecast residuals as a simple anomaly detector, setting the threshold on normal data and reporting false alarms and detection delay.

**Topics** (notes may go deeper; the deck is budgeted at about 48 slides for 90 minutes including a 20-minute demo):
- **Time series as supervised learning.** `fit`/`predict`, parameters against settings (hyperparameters), the held-out score. RECALL L7's `lstsq` on a design matrix, then change one thing: the target becomes `y[t+h]`, built with `shift(-h)` inside the run. State what is known at forecast time, including that an exogenous input's *future* is not known unless it is a plan (a setpoint schedule, a weather forecast).
- **What makes a series forecastable.** Autocorrelation against lag in minutes; stationarity; trend and seasonality named once with the examples where they matter (load, retail), and why a controlled plant mostly has neither; differencing; prices against returns. Measured on the Rieth fault-free file: seven continuous TEP channels (`xmeas_5, 6, 9, 12, 14, 15, 17`) are white noise around a setpoint, where persistence is about √2 ≈ 1.41 times worse than the mean at every horizon and no model finds skill. That is the same shape as asset returns, so the finance contrast comes out of our own plant.
- **Baselines and skill.** Persistence (the naive forecast, which L7 used without naming it), the mean, seasonal naive and drift named for other domains. Skill score against the better baseline, MASE (Hyndman and Koehler 2006). Measured on reactor pressure `xmeas_7`, test runs 401-500: persistence RMSE 1.92 kPa at h=1 against 7.51 for the mean, crossing between h=10 and h=20 (8.87 against 7.61 at h=20). **Case studies:** Meese and Rogoff (1983) on exchange rates and the M4 competition. Verify every figure before quoting.
- **Model families and multi-step strategies.** AR/ARX (recall), ARIMA named with `statsmodels`, lag-feature regression (ridge, gradient boosting) as the reduction that lets any regressor forecast. Direct (one model per horizon) against recursive (iterate the one-step model). Measured, AR(10) ridge on `xmeas_7`: direct 7.17 against recursive 8.10 at h=40, where the recursive forecast is *worse than the mean* (7.65). `Pipeline` with a scaler matters now because ridge, unlike `lstsq`, depends on scale.
- **Evaluating on time.** Rolling origin (backtesting), `TimeSeriesSplit(gap=...)` with gap at least h, splitting by run when there are many series. **Be honest about what the data shows:** with ridge on a table pooled from 200 independent runs, a random-row split and a by-run split give the same score (4.74 against 4.71 kPa at h=10). The leak appears on a single series. Measured one run at a time (runs 1-10, h=10, mean CV RMSE): random forest 4.32 under shuffled `KFold` against 6.92 under `TimeSeriesSplit` (7.23 with gap=10), ridge 4.82 against 6.67, with persistence at about 6.1. The leaky split says both models beat persistence comfortably; the honest one says neither does. Numbers are printed by `lectures/l08/figures/make_figures.py`. Finance names: look-ahead bias, survivorship bias.
- **Residuals: diagnosis and detection.** Residuals of a good model look like white noise (ACF). A residual far outside its normal spread is a signal: threshold on held-out fault-free runs, false-alarm rate (a 1 % threshold on 3-minute samples is about 4.8 false alarms a day), detection rate and delay on one fault run. This is the only anomaly detection in the session.
- **Limitations.** Regime change and nonstationarity; feedback (L7's closed loop; forecasts that move the market); faults absent from training; a horizon ceiling set by the process's own dynamics.

**Live demo / in-class activity:** `l08-forecasting.ipynb`, about 20 minutes, placed after the evaluation section. Build the h-step table for `xmeas_7` with L7's `build_arx` extended by `h` (so it is self-contained if a student missed L7; L7 no longer writes a Parquet file); score persistence and the mean across horizons; fit direct ridge and recursive; plot skill against horizon; then shuffled `KFold` against `TimeSeriesSplit` on one run with a random forest, and watch the leaky score beat persistence.

**Clicker questions (3):** a random-walk price where persistence beats your model; R² 0.99 at h=1 and what to compare it to; which test rows a target `y[t+h]` contaminates without a gap.

**Engineering framing / dataset:** Rieth et al. (2017) fault-free training file (500 runs × 500 samples, 3-minute interval), reactor pressure `xmeas_7` for the session, one Rieth faulty run for the detector. A4 uses a different channel (stripper temperature `xmeas_18`, where ridge beats the better baseline by up to 47 %) so the assignment is not a copy of the demo.

**Readings & resources:**
- Hyndman and Athanasopoulos, *Forecasting: Principles and Practice*, 3rd ed., chapter 5 (baselines, accuracy, time-series cross-validation) and section 9.1 (stationarity). Free.
- Hyndman and Koehler (2006), MASE.
- Taieb et al. (2012), recursive against direct strategies.
- scikit-learn `TimeSeriesSplit` (the `gap` parameter) and the lagged-features example; skforecast on recursive and direct forecasters.
- Bergmeir, Hyndman and Koo (2018): K-fold is valid for purely autoregressive models with uncorrelated errors, which is the boundary of the "never shuffle" rule and matches the ridge measurement above.
## Who owns which term
Each of these was previously introduced two or three times across L7, L8 and L9. Introduce once, then recall.

- **L7 introduces:** dynamic vs steady state, lag feature, leakage, zero-order hold, first-order process, time constant, steady-state gain, manipulated variable, least squares, ARX(1,1), persistent excitation, identifiability, closed-loop identification, naive forecast.
- **Terminology is deliberate, and a research pass settled it.** Three terms in an earlier draft were wrong or invented: *realizability* (a real control term for a different thing), *a process with memory* (loaded, since LSTM means Long Short-Term Memory) and *lag table* (nobody calls it that). The replacements are the words a ChemE with control and ML already owns. The session also now names the system-identification vocabulary it had been performing silently: it was doing ZOH discretization without saying so, the valve-never-moved case is a persistent-excitation failure, and the controller case is closed-loop identification.
- **L8 introduces:** supervised learning, `fit`/`predict`, parameter, setting (hyperparameter), held-out score, forecast horizon, autocorrelation, stationarity, differencing, baseline, persistence (naming L7's naive forecast), skill score, MASE, direct and recursive forecasting, cross-validation, rolling origin (forward chaining), `TimeSeriesSplit` and its gap, look-ahead bias, residual, threshold, false-alarm rate, detection delay, `Pipeline`.
- **Moved from L8 to L7 during the L7 rebuild, so L8 should now RECALL rather than introduce:** ARX, NARX, the regression vector and design matrix, the naive forecast, and the name (though not the mechanism) of train-test contamination. The `rolling_*` syntax also moved to L7; L8 keeps the choice of window length, which is the part that needs a score.
- **L9 introduces:** held-out test, `GroupKFold`, nested CV, bias and variance, learning curves, the choice of metric, classification metrics, the one-standard-error rule.

`course/modules/wk05.md` and `lectures/l09/notes.md` currently introduce `TimeSeriesSplit`, forward chaining and the baseline ladder as if new. Those need rewriting as backward references once L8 exists.

**Notation.** Use `t` for the sample index throughout, never `k`, so it cannot collide with `K` the gain when spoken aloud. Avoid the word "order" in L7 entirely: it means three different things across this material.

## Assignment
**A4 - An h-step forecaster for a plant channel.** Released L8 (09-21), due 09-28, 6 points on Canvas, graded by `a04-evidence.py` in the A3 pattern plus a short report a person reads. Rieth fault-free training file, stripper temperature `xmeas_18`, horizons h = 1, 5, 10, 20, 40. Required: persistence and mean baselines, a direct ridge model per horizon, a recursive model, splits by simulation run with a rolling origin and a gap of at least h, a skill-against-horizon table, and a shuffled-against-time-ordered comparison on a single run. Stretch: a residual detector on one faulty run. The live A3 stream is not used: ten minutes is one plant day and carries disturbances, which is too little data for an honest multi-horizon evaluation. DVC, MLflow and the scaler A/B from the old spec are gone. Full spec: `assignments/a04.md`.
## What moved out of this week, and where it went
- **Data versioning (DVC, content hashing) and run lineage (git SHA, data hash, seed).** The deleted L8 was their only home. `lectures/l10/notes.md` already links `../l08/notes.md` three times for exactly this and already teaches what to log alongside a run, so L10 is the natural place. **Decide before L10 is written, and fix those cross-references either way.**
- **Grouped splits and the leakage taxonomy.** L9 owns them, with the airfoil measurement behind it.
- **Data documentation (datasheets, data cards) and data-centric iteration.** Not sited anywhere now. Cheap to drop, but it should be a decision rather than an oversight.
- **Physical and domain features: dimensionless groups, calibration corrections, FFT band energy.** Dropped with the "and physical data" half of L7's old title. No assignment required them, the existing notebook never ran the band-energy code, and the course has no vibration dataset. The Mars Climate Orbiter case study went with them; the reversed-causality case replaces it.
- **Aliasing.** A genuine delight and worth one slide somewhere, but it is a fourth idea in L7. It belongs with L6's sampling material or as a practice-module item.
- **Calendar and seasonality features (day of week, hour of day, cyclical encodings, holidays, STL decomposition, ARIMA differencing).** Deliberately dropped, not overlooked. A continuous plant has no retail-style weekly seasonality; its diurnal effect is ambient temperature, and the right treatment is to put the measured channel in the table rather than regress on the clock. Name ARIMA once in a reading annotation so students can find the literature.

## Two holes this week does not cause but does expose
Neither belongs in L7, and both should be fixed while the notes are being written.

- ~~**Resampling to a grid, gap semantics and fill policy live only in A3 and in one paragraph of L6's demo narrative.**~~ **Closed by L7's four-operations block**, items (a) and (b), which carry the interval count, `upsample`, and the asymmetric fill rule (forward-fill the actuator, never the measurement, keep the mask). Item (d) carries the analyser hold. The bank can now test all three: `l07-q12` is the fill question. A3 still states the rule better than most textbooks, that an empty analyser cell means no new analysis yet while an empty continuous cell means a lost message, and the notes point at the same asymmetry. The remaining gap is the plant-stream numbers from L6's demo narrative (34 continuous tags on a 180-second grid against 19 analysers on an hourly cycle, roughly 1% and 58% empty), which are still only in demo prose. Worth one paragraph in L6's notes when L6 is next revised, and it is no longer urgent.
- **`lectures/l12/notes.md` cross-references `../l08/notes.md` for the C-MAPSS dataset and for the leakage lesson.** The new L8 contains neither. Fix those links when L8 is rewritten.
- **Lag-order and sampling-interval selection as design decisions.** Selection needs a criterion, a criterion needs a held-out number, and that arrives in L8 and L9. In L7 the lag depth is one, stated as a starting point rather than defended, and the sampling interval is a sanity check rather than a choice. The rolling-window length is in the same position: L7 states the rule of thumb (longer than the lag depth) and L8 owns choosing it.

## Teaching notes / common pitfalls
- The sign of the shift is the first thing to get wrong. `shift(1)` puts the previous row's value on this row; `shift(-1)` reads the future. Show both with timestamps printed, and give students the leakage sentence to say out loud for every column they build.
- Subtract the mean, the way they already do in control. On a channel with a large operating-point offset, skipping it returns a badly wrong `a`, and it will happen in the live demo if nobody checks.
- The rows at the head of each run are not missing data. Their past lies outside the record, so drop them. `fill_null(0)` invents a reactor at zero degrees.
- Students will add lag columns until the table is wide. Lags of a smooth signal correlate above 0.99, so the coefficients come back enormous with alternating signs while the predictions still look fine. Print the correlation matrix once and let them see it. Adding a rolling mean whose window fits inside the lag depth makes the problem worse, and `lstsq` picks one of infinitely many answers without warning anyone.
- `faultNumber` in the table is the answer to the question being asked. It will happen.
- The loop plot needs an input that moves up and back down. A monotone ramp gives a displaced curve, not a loop, so check the chosen window before class.
- Composition analysers report on a slow cycle and hold their previous value in between, so a lag-1 difference on those channels is exactly zero most of the time. This is now a slide, a figure and a demo cell, item (d) of the four-operations block, and it is the most process-specific thing in the session: the flat fractions come out at exactly 0.500 and 0.800, which are the 6- and 15-minute analyser cycles on a 3-minute grid. Let the room read the two plateaus off the bar chart before you say what they are.
- L7 fits one least-squares solve and reports a time constant. L8 fits models and reports errors. If L7 starts reporting scores it has taken L8's session, and students meet metrics before anyone has defined one.
- Both sessions will run long. L7's abort order: cut the coefficient-perturbation cell first, then the persistence slide and cell (email the number, L8 only needs the value). Inside the four-operations block, item (c) goes first and is replaced by its one sentence. Do not cut the lag plot: it is the cheapest diagnostic in the course and it is the visual derivation of the whole session. Never cut the reversed-causality block; it is the best ten minutes of the week.
